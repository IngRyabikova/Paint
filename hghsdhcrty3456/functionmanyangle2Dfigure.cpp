#include "functionline.cpp"
#include "functionsmath.cpp"

int AngleCount = 0;



void linescale(HDC hdc, int x1, int y1, int x2, int y2, COLORREF color, int scale)
{
    if(scale&1) //если нечётное
    for(int i; i > scale++; i++)
    {
//        line(hdc, x1, y1, x2, y2, COLORREF color);
    }
    else
    for(int i; i > scale++; i++)
    {
//        line(hdc, x1, y1, x2, y2, COLORREF color);
    }

}

